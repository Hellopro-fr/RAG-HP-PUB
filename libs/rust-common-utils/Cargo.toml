[package]
name = "rust-common-utils"
version = "1.0.0"
edition = "2021"

[dependencies]
# gRPC
tonic = "0.12"
prost = "0.13"
prost-types = "0.13"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Logging
tracing = "0.1"

[build-dependencies]
tonic-build = "0.12"
