# Trivy secret-scanning config — chargé par `trivy fs`/`image` (chemin par défaut --secret-config).
# Allowlist de FAUX POSITIFS secret VÉRIFIÉS uniquement. Ne JAMAIS y mettre de vrais secrets.
allow-rules:
  - id: gcp-sa-json-template-p12-converter
    description: >-
      p12_to_json.py construit un dict JSON de service-account a partir d'un P12 monte au
      runtime — aucune cle en dur. Faux positif F-HP-SEC-013 (verifie 2026-08-27 : 0 cle
      dans l'historique git). Ref : Docs/DevSecOps/TICKETS/001-MIGRATION-ARCHI/remediation-REM5-secret-gcp.md
    path: apps-microservices/mcp-google-analytics-service/p12_to_json\.py
