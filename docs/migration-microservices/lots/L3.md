# Lot L3 — prix — première écriture Milvus en production

> **Lot d'exécution 3/7** · risque **Moyen** · date proposée **mer 23/09** *(à confirmer par la décision du 18/09)*.
> Procédure commune : [`../procedure-bascule-un-lot.md`](../procedure-bascule-un-lot.md) · plan : [`../plan-bascule-par-lots.md`](../plan-bascule-par-lots.md) · suivi : [`../suivi-bascule-par-lots.md`](../suivi-bascule-par-lots.md).
> ⚠️ **L1…L7 sont des lots d'exécution** ; ils ne couvrent que les priorités **P1, P2, P3** de l'inventaire. Ne pas confondre avec P1…P10.

> 🔴 **Couplage par disque partagé (F-HP-MIG-008)** : ces services écrivent un fichier de suivi **par message** dans `/app/tracking`, monté sur un disque hôte que `qc-tracking-service` (reste sur la VM) relit et expose. **Aucun volume dans les manifestes GKE** → les fichiers iraient dans le pod, éphémères, invisibles du tracking. **Décision LEAD avant ce lot** : accepter la perte du tracking fichier (logs pod à la place) + `emptyDir` pour sécuriser l'écriture — ou reporter. Vérifier au pré-flight que l'écriture ne plante pas si `tracking/` n'existe pas dans le conteneur.

## Composition

| Service (nom `docker-compose`) | Réplicas VM | Déploiement GKE (nom exact) | Secret à repointer | Cible réplicas GKE au départ |
|---|:--:|---|---|:--:|
| `prix-extraction-produits` | 1 | `prix-extraction-produits` | `prix-extraction-produits-rabbitmq` | 1 |
| `prix-extraction-message` | 1 | `prix-extraction-message` | `prix-extraction-message-rabbitmq` | 1 |
| `prix-extraction-devis` | 1 | `prix-extraction-devis` | `prix-extraction-devis-rabbitmq` | 1 |
| `prix-caracterisation` | 1 | `prix-caracterisation` | `prix-caracterisation-rabbitmq` | 1 |
| `prix-milvus-processor-service` | 4 | `prix-milvus-processor` | `prix-milvus-processor-secrets` | 2 |

**5 services · 8 conteneurs VM à arrêter · cible de départ 6 pods GKE** (≈ la moitié des réplicas VM, ajustée ensuite sur la profondeur des queues).

## Ce qui bascule dans ce lot

- broker RabbitMQ dev→prod (`10.0.1.217` → `10.0.1.216`)
- sortie internet Cloud NAT (IP `35.233.35.8`, whitelistée ECRITEL)
- Milvus **lecture** dev→prod (`10.0.1.51`)
- Milvus **écriture** dev→prod (`10.0.1.51`) — snapshot la veille

## À savoir, service par service

| Service | Vigilance |
|---|---|
| `prix-extraction-produits` | Sortie LLM + API HelloPro. |
| `prix-extraction-message` | Sortie LLM + API HelloPro. |
| `prix-extraction-devis` | Sortie LLM + API HelloPro. |
| `prix-caracterisation` | Lecture Milvus **directe** (pymilvus, connexion à la demande) → `ZILLIZ_*` vers `10.0.1.51`. |
| `prix-milvus-processor-service` | **Writer Milvus** — première écriture prod de la série. Secret **multi-clés** (`rabbitmq-url`, `zilliz-user`, `zilliz-password`) → `kubectl patch` obligatoire. ⚠️ Nom GKE **sans** `-service`. |

## Prérequis spécifiques au lot (en plus de la procédure commune)

- ✅ **Décision LEAD du 21/09 (F-HP-MIG-008)** : perte du tracking fichier acceptée, logs pod en remplacement. **Reste à faire avant le lot** : `emptyDir` sur `/app/tracking` dans les manifestes (dépôt infra, `kubectl apply` sur le shadow, commit), et vérifier que l'écriture ne plante pas sans le répertoire.
- Snapshot **Milvus prod** la veille.

## Ce qu'on attend du LEAD et des devs concernés

- Pré-contrôle de chaque service remis **avant la veille** (fiche du [`guide-pre-check-service.md`](../guide-pre-check-service.md)).
- Un référent joignable pendant la fenêtre (2 h) et le lendemain matin.
- Après bascule : un scénario métier de bout en bout sur la chaîne, et confirmation que les écritures attendues sont là — **et pas en double**.

## Feuille de lot

| Étape | Heure | Résultat | Par |
|---|---|---|---|
| Pré-flight (contexte, listes dérivées, comptes) | | | |
| État de référence broker | | | |
| Arrêt des jumeaux VM | | | |
| Repointage des secrets + rollout | | | |
| Broker : consumers > 0, backlog décroît | | | |
| Montée en réplicas (cible de départ) | | | |
| Validation fonctionnelle LEAD/dev | | | |
| GO observation 24 h | | | |
| **Décision J+1 matin : lot suivant / prolonger / rollback** | | | |

## P0 du 23/09 — état

| Prérequis | Résultat |
|---|---|
| GO | ✅ LEAD + dev (même dev QC/prix), 23/09 ~7h20 Paris, dans le prolongement du GO L2 |
| Re-scan Trivy (4 prix `tracking-2026-09-23` + `prix-milvus-processor:shadow-2026-09-15`) | ✅ 0 CRITICAL corrigeable (CRITICAL restantes = `CVE-2026-43185`, VEX-01) — `data/rescan-2026-09-23/` |
| `emptyDir` `/app/tracking` + push tracking (F-HP-MIG-008/010) | ✅ 4 prix en shadow, variables `TRACKING_*` présentes, `restarts=0` |
| Parité des valeurs VM/GKE | ✅ `HP_TOKEN`/`GEMINI_API_KEY` identiques ; **`prix-caracterisation` corrigé** (F-HP-MIG-011 : `DEEPSEEK_API_KEY` + `ZILLIZ_*` manquants) ; `prix-extraction-*` n'utilisent pas DeepSeek |
| Milvus prod | ✅ identifiants SM `platform-zilliz-user` (5 car.) / `platform-zilliz-password` (16 car.) = `.env` VM ; `milvus-prod.hello.dev.private.com` → `10.0.1.51`, port 19530 ouvert **depuis un pod GKE** |
| Référence données | ✅ collection `prix` = **23 483 entités**, clé `id` INT64 `auto_id` ; `max id = 467759069053297382` à 06:58:17 UTC (à relever de nouveau à P2 = borne du rollback) |
| Sauvegarde | ✅ `gs://milvus-cluster-data-backup/milvus-backups/daily/daily_20260923_020000` (02:04 UTC, `meta/` + `binlogs/`, `prix` présent), série quotidienne continue depuis le 25/08 |

**Filet données retenu** (plutôt que des snapshots de disques etcd/minio/pulsar, crash-consistents et lourds à restaurer) : `prix-milvus-processor` est insert-only dans la seule collection `prix` et, après P2, seul GKE écrit → rollback données = suppression des entités `id > max_id(P2)` ; restauration complète possible depuis la sauvegarde de la nuit.

## Bascule du 23/09 — feuille

| Étape | Heure (UTC) | Résultat | Par |
|---|---|---|---|
| P1 pré-flight | 07:20 | 5 déploiements · 5 secrets broker (4 `-rabbitmq` + `prix-milvus-processor-secrets`) · **8 conteneurs VM** (4 × `prix-milvus-processor-service`), aucun `siteweb`/`traitement` ✅ | DSO |
| Photo broker | 07:27:55 | 124 files | DSO |
| `max id` Milvus `prix` avant arrêt | 07:28:07 | 23 483 lignes · `467759069053297382` | DSO |
| P2 arrêt des 8 jumeaux | 07:28:07→07:28:38 | 8 × `Exited (137)` | DSO |
| **`max id` après arrêt (borne du rollback données)** | 07:28:43 | **23 483 lignes · `467759069053297382`** (conteneur jetable, mêmes variables `ZILLIZ_*`) | DSO |
| Files du lot (différence broker) | 07:29 | 5 : `insertion_prix_queue`, `prix_caracterisation_queue`, `prix_devis/message/produits_processing_queue` | DSO |
| P3 secrets (5 broker + `zilliz-user`/`zilliz-password` prod) + `ZILLIZ_URI` prod sur 2 déploiements + rollout ×5 + `prix-milvus-processor` à 2 | 07:30:40→07:31:28 | empreintes Milvus `ec7955cf`/`7384d49e` = SM `platform-zilliz-*` = VM ✅ | DSO |
| P4 broker | 07:32:17 | `insertion_prix_queue 0 0 2`, 4 autres `0 0 1` → **bascule ~4 min 10** | DSO |
| Milvus prod depuis les pods | 07:33 | `prix-milvus-processor` et `prix-caracterisation` : URI `milvus-prod.hello.dev.private.com`, `prix` = 23 483, `restarts=0`, 0 erreur au démarrage (5 pods) | DSO |
| P6 test réel prix (catégorie 2005786) | 12:27→13:12 | `prix-extraction-message` 14/164 succès → **28 insertions** ; `prix-extraction-devis` **KO** (gRPC `database-recherche-service` introuvable, F-HP-MIG-012) → `scale 0` 12:50, sonde proxy OK, 3 adresses gRPC → proxy, rejoué 12:54 : **41/41, 43 insertions** ; `prix-extraction-produits` 0/0 ; `prix-caracterisation` : 5 × 200 HelloPro, 10 × 200 DeepSeek, **lecture Milvus prod** (280 points), caractérise les 28 lignes insérées par GKE (23 × 200 HelloPro, 20 × 200 DeepSeek à 13:15, en cours) ; **fichier de tracking visible dans `qc_tracking-service`** ; + 30 insertions `siteweb` venues de la VM (`prix-extraction-siteweb` hors lot, par la file). **101 lignes, 0 doublon, 0 lead réinséré** | dev · DSO |
| Manifestes alignés sur le cluster (`ZILLIZ_URI` prod, `prix-milvus-processor` ×2, gRPC devis) | 13:05 | `kubectl diff` = 0 écart (ordre des variables seul) ; commit infra `448e0bbc` | DSO |
| Décision J+1 | jeu 24/09 9h30 Paris | | LEAD · DSO |

**Rollback données** (si écriture incorrecte ou dupliquée) : supprimer dans `prix` les entités `id > 467759069053297382` (insertions GKE seulement) ; restauration complète possible depuis `daily_20260923_020000`.
