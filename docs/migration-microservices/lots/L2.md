# Lot L2 — QC — sortie internet sous charge réelle

> **Lot d'exécution 2/7** · risque **Faible** · date proposée **mar 22/09** *(à confirmer par la décision du 18/09)*.
> Procédure commune : [`../procedure-bascule-un-lot.md`](../procedure-bascule-un-lot.md) · plan : [`../plan-bascule-par-lots.md`](../plan-bascule-par-lots.md) · suivi : [`../suivi-bascule-par-lots.md`](../suivi-bascule-par-lots.md).
> ⚠️ **L1…L7 sont des lots d'exécution** ; ils ne couvrent que les priorités **P1, P2, P3** de l'inventaire. Ne pas confondre avec P1…P10.

## Composition

| Service (nom `docker-compose`) | Réplicas VM | Déploiement GKE (nom exact) | Secret à repointer | Cible réplicas GKE au départ |
|---|:--:|---|---|:--:|
| `qc-caracterisation` | 2 | `qc-caracterisation` | `qc-caracterisation-rabbitmq` | 1 |
| `qc-enrichissement` | 2 | `qc-enrichissement` | `qc-enrichissement-rabbitmq` | 1 |
| `qc-equivalence` | 2 | `qc-equivalence` | `qc-equivalence-rabbitmq` | 1 |
| `qc-generation-caracteristiques` | 2 | `qc-generation-caracteristiques` | `qc-generation-caracteristiques-rabbitmq` | 1 |
| `qc-generation-question1` | 2 | `qc-generation-question1` | `qc-generation-question1-rabbitmq` | 1 |
| `qc-generation-question2an` | 2 | `qc-generation-question2an` | `qc-generation-question2an-rabbitmq` | 1 |
| `qc-generation-valeurs` | 2 | `qc-generation-valeurs` | `qc-generation-valeurs-rabbitmq` | 1 |

**7 services · 14 conteneurs VM à arrêter · cible de départ 7 pods GKE** (≈ la moitié des réplicas VM, ajustée ensuite sur la profondeur des queues).

## Ce qui bascule dans ce lot

- broker RabbitMQ dev→prod (`10.0.1.217` → `10.0.1.216`)
- sortie internet Cloud NAT (IP `35.233.35.8`, whitelistée ECRITEL)

## À savoir, service par service

| Service | Vigilance |
|---|---|
| `qc-caracterisation` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |
| `qc-enrichissement` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |
| `qc-equivalence` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |
| `qc-generation-caracteristiques` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |
| `qc-generation-question1` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |
| `qc-generation-question2an` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |
| `qc-generation-valeurs` | Sortie Cloud NAT `35.233.35.8` vers le LLM et `api.hellopro.fr`. Secrets `platform-llm-hp-secrets` réels depuis le 16/09. |

## Prérequis spécifiques au lot (en plus de la procédure commune)

- Confirmer les quotas des LLM (Gemini, DeepSeek) : la charge de 7 services arrive d'un coup depuis une nouvelle IP.

## Ce qu'on attend du LEAD et des devs concernés

- Pré-contrôle de chaque service remis **avant la veille** (fiche du [`guide-pre-check-service.md`](../guide-pre-check-service.md)).
- Un référent joignable pendant la fenêtre (2 h) et le lendemain matin.
- Après bascule : un scénario métier de bout en bout sur la chaîne, et confirmation que les écritures attendues sont là — **et pas en double**.

## Feuille de lot

| Étape | Heure | Résultat | Par |
|---|---|---|---|
| Pré-flight (contexte, listes dérivées, comptes) | | | |
| État de référence broker | | | |
| Arrêt des jumeaux VM | | | |
| Repointage des secrets + rollout | | | |
| Broker : consumers > 0, backlog décroît | | | |
| Montée en réplicas (cible de départ) | | | |
| Validation fonctionnelle LEAD/dev | | | |
| GO observation 24 h | | | |
| **Décision J+1 matin : lot suivant / prolonger / rollback** | | | |

