# Lot L6 — transverses sensibles — GPU et back-office

> **Lot d'exécution 6/7** · risque **Fort** · date proposée **mar 29/09** *(à confirmer par la décision du 18/09)*.
> Procédure commune : [`../procedure-bascule-un-lot.md`](../procedure-bascule-un-lot.md) · plan : [`../plan-bascule-par-lots.md`](../plan-bascule-par-lots.md) · suivi : [`../suivi-bascule-par-lots.md`](../suivi-bascule-par-lots.md).
> ⚠️ **L1…L7 sont des lots d'exécution** ; ils ne couvrent que les priorités **P1, P2, P3** de l'inventaire. Ne pas confondre avec P1…P10.

> ⚠️ **Fenêtre tarifaire DeepSeek** (`libs/common-utils/.../fenetre_tarifaire.py`, partagée par 4 services : `nettoyage-bruit-ocr-service`, `QC-caracterisation`, `template-llm-service`, `QC-fabricant-reference`) : désabonnement volontaire **01h-04h et 06h-10h UTC** (**04h-07h et 09h-13h locales**), surchargeable par `DEEPSEEK_FENETRES_PLEINES`. Un `consumers=0` à ces heures est **nominal**. **Basculer ces services hors fenêtre** (après 13h locales) pour que la preuve broker soit immédiate.
> `template-llm-service` porte la fenêtre — contrainte déjà notée, confirmée par le code partagé.

## Composition

| Service (nom `docker-compose`) | Réplicas VM | Déploiement GKE (nom exact) | Secret à repointer | Cible réplicas GKE au départ |
|---|:--:|---|---|:--:|
| `embedding-service` | 4 | `embedding-service` | `embedding-service-rabbitmq` | 2 |
| `template-llm-service` | 4 | `template-llm-service` | `template-llm-service-rabbitmq` | 2 |
| `webhook-service` | 1 | `webhook-service` | `webhook-service-rabbitmq` | 1 |

**3 services · 9 conteneurs VM à arrêter · cible de départ 5 pods GKE** (≈ la moitié des réplicas VM, ajustée ensuite sur la profondeur des queues).

## Ce qui bascule dans ce lot

- écriture back-office PHP (HMAC) — message contrôlé
- broker RabbitMQ dev→prod (`10.0.1.217` → `10.0.1.216`)
- sortie internet Cloud NAT (IP `35.233.35.8`, whitelistée ECRITEL)
- gRPC GPU via l'enabler `10.11.0.2:1505x` (déjà validé en shadow, inchangé)

## À savoir, service par service

| Service | Vigilance |
|---|---|
| `embedding-service` | gRPC embedding `15052`. Publie vers les queues des writers (L4). ⚠️ **≠ `api-embedding-service`** (HTTP, Cloud Run, hors lots). |
| `template-llm-service` | ⚠️ **Se désabonne volontairement de `llm_templating_queue` entre 06h et 10h UTC** (tarif DeepSeek). **Basculer après 13h locales** ; un `consumers=0` avant cette heure n'est pas un échec. |
| `webhook-service` | **Écrit vers le back-office PHP en HMAC** (`KEY_WEBHOOK` réel depuis le 16/09) — **jamais exercé** sur données réelles. Seul conteneur VM **sans** préfixe `rag-hp-pub-`. Un message contrôlé avant d'ouvrir. |

## Prérequis spécifiques au lot (en plus de la procédure commune)

- Préparer **un message contrôlé** pour le webhook, et un moyen de vérifier son effet côté back-office PHP avec le LEAD.
- **Fenêtre après 13h00 locales** (10h UTC) à cause du désabonnement horaire de `template-llm-service`.

## Ce qu'on attend du LEAD et des devs concernés

- Pré-contrôle de chaque service remis **avant la veille** (fiche du [`guide-pre-check-service.md`](../guide-pre-check-service.md)).
- Un référent joignable pendant la fenêtre (2 h) et le lendemain matin.
- Après bascule : un scénario métier de bout en bout sur la chaîne, et confirmation que les écritures attendues sont là — **et pas en double**.

## Feuille de lot

| Étape | Heure | Résultat | Par |
|---|---|---|---|
| Pré-flight (contexte, listes dérivées, comptes) | | | |
| État de référence broker | | | |
| Arrêt des jumeaux VM | | | |
| Repointage des secrets + rollout | | | |
| Broker : consumers > 0, backlog décroît | | | |
| Montée en réplicas (cible de départ) | | | |
| Validation fonctionnelle LEAD/dev | | | |
| GO observation 24 h | | | |
| **Décision J+1 matin : lot suivant / prolonger / rollback** | | | |

## Préparation (25/09) — pré-contrôle vérifié par le DSO, en attendant le dev

Essai sur ce lot (décision du 25/09) : le DSO vérifie lui-même dans le code ce qui peut l'être (points 1 et 2), le dev répond sur le reste (3 et 5) ; le point 4 est tranché : **la valeur de la VM prime**.

| Point | Constat DSO (code `prod` = source des images GKE, et VM) |
|---|---|
| 1. Fichiers | `embedding-service`, `webhook-service` : aucune écriture. `template-llm-service` : `os.makedirs('recovery_data')` au démarrage (`app/main.py:29`), **jamais écrit ensuite** ; le tokenizer `AutoTokenizer.from_pretrained("deepseek-ai/DeepSeek-R1")` (`app/core/processor.py:110`) **télécharge depuis Hugging Face et met en cache sur le disque du conteneur** à chaque démarrage → éphémère, sans enjeu sur GKE, mais dépendance réseau sortante (déjà réussie en shadow) |
| 2. Variables | `embedding-service` : `RABBITMQ_URL`, `PREFETCH_COUNT` (VM `2` = défaut), 🔴 **`PROCESS_TIMEOUT` (VM `360`, défaut code `240`, absent du manifeste)** — le code exige `PROCESS_TIMEOUT > GRPC_TIMEOUT` (`300`) pour que le délai gRPC parte avant le plafond (spec livelock du 02/07, `app/messaging/consumer.py:16-22`) : sans la variable, un message long serait coupé à 240 s → **à ajouter au manifeste (360) avant L6**, `EMBEDDING_SERVICE_URL` (client gRPC `common_utils`, VM `embedding-model-service:50052` → GKE `10.11.0.2:15052` ✅), `SERVICE_NAME`/`GRPC_TIMEOUT` (défauts). `template-llm-service` : `RABBITMQ_URL`, `LLM_SERVICE_URL` (VM `llm-service:50051` → GKE `10.11.0.2:15051` ✅), `DEEPSEEK_FENETRES_PLEINES` (non passé : défaut). `webhook-service` : `RABBITMQ_URL`, `KEY_WEBHOOK`, `WEBHOOK_BATCH_SIZE` (défaut 50), `WEBHOOK_BATCH_TIMEOUT_S` (défaut 5.0), **`WEBHOOK_UPDATE_PRODUIT_URL`** (lu par `common_utils/autres/CollectionWebhook.py`, absent du manifeste — valeur VM à relever) |
| 3. Double traitement | ⬜ réponse dev (le back-office accepte-t-il un webhook deux fois ?) |
| 4. `KEY_WEBHOOK` | **La valeur VM prime** (décision 25/09). Mesure dans les conteneurs VM : **10 caractères, `21387e2c`** (identique sur les 3 services) — l'écart « SM 10 / VM 14 » de F-HP-SEC-023 venait de la lecture du fichier `.env`, pas du conteneur. À comparer à SM `platform-key-webhook` ; le secret K8s porte encore un placeholder |
| 5. Message contrôlé | ⬜ réponse dev |

**P0 VM (25/09)** : 9 conteneurs (`embedding-service` 4, `template-llm-service` 4, `webhook-service` 1 — nommé `/webhook-service`, sans préfixe) ; broker `ce902988` partout. **`embedding-service` exécute le code du checkout VM** (bind-mount `/home/devhp/RAG-HP-PUB/apps-microservices/embedding-service/app → /app/embedding_service`) : **parité vérifiée**, aucun écart entre la source de l'image GKE (`a4bee8e5`) et le checkout VM (`10ded3e7`) sur `apps-microservices/embedding-service/app` et les modules `common_utils` d'embedding. Tant que les jumeaux existent, pas de `git pull` sur la VM.

🔴 **Découverte (F-HP-SEC-026)** : les URL de webhook sont **codées en dur vers `webhook.site`** (service public de test) pour les 6 collections (`common_utils/autres/CollectionWebhook.py`, identique sur `prod` et `features/poc`, dernier changement 2025-08-28 / 2026-03-26), avec un `TODO` « modification url à notifier ». Seul le mode `update` produit peut viser une autre URL (`WEBHOOK_UPDATE_PRODUIT_URL`). À faire confirmer par le LEAD avant L6 : où partent réellement les webhooks de prod aujourd'hui ?

**Réponses trouvées par le DSO (25/09, suite)** : `KEY_WEBHOOK` SM = VM (`21387e2c`, 10 car.) → **F-HP-SEC-023 clos** (faux écart), clé K8s à alimenter depuis SM ; `GRPC_TIMEOUT` VM `300` = défaut ; **cible réelle des webhooks** (logs VM 24 h) : 2 133 appels vers `https://www.hellopro.fr`, 1 084 × `status: 200`, 0 vers `webhook.site` → F-HP-SEC-026 ramené à **latent** ; `WEBHOOK_UPDATE_PRODUIT_URL` VM → `https://www.hellopro.fr/…` (valeur exacte à relever pour le manifeste).

**Mécanique réelle du webhook (lue dans le code, 25/09)** : `webhook-service` **n'envoie que les messages `mode=update`** et acquitte les autres sans rien faire (`app/messaging/consumer.py:157`) — sur 7 jours VM : 40 304 messages `mode=none` ignorés, **6 256 appels, tous vers `https://www.hellopro.fr`**. Pour les produits, l'URL `update` vient de **`WEBHOOK_UPDATE_PRODUIT_URL`** (VM : `https://www.hellopro.fr/fichiers_communs_bo_front/rag/webhook/webhook_update_produit_scrapping.php`, sans query) ; à défaut, repli sur la table `CollectionWebhook` = **`webhook.site`**. Le conteneur VM a la même table que `prod` (image du 07/04) : l'image GKE est bonne.
→ **Variable indispensable** : sans `WEBHOOK_UPDATE_PRODUIT_URL` sur GKE, tout le trafic réel partirait vers `webhook.site`.
→ **Clé et URL du webhook posées en P3 seulement** (pas en shadow) : en shadow, le webhook consomme le broker dev ; lui donner la vraie clé et la vraie URL enverrait au back-office prod, signés, les messages `update` de dev. `PROCESS_TIMEOUT=360` (embedding) est appliqué en shadow dès le 25/09.
→ Reste au dev : point 3 (le back-office accepte-t-il un même `update` deux fois ?) et point 5 (message contrôlé, sinon trafic réel).

**25/09 (fin)** : `PROCESS_TIMEOUT=360` appliqué en shadow sur `embedding-service` (`kubectl diff` = ajout seul, pod 0 restart, commit infra `e97e2f65`, 14 non poussés) ; webhook shadow : **0 tentative d'envoi en 72 h**. **L6 prêt côté DSO** : scripts `l6_broker.sh` et `l6_p3.sh` (garde-fous broker et `key-webhook` `21387e2c`, copies `-prel6` des 3 secrets + `platform-llm-hp-secrets`, patch et relecture, `WEBHOOK_UPDATE_PRODUIT_URL` posée par `set env`, rollout). Manifeste `webhook-service` à aligner et commiter après P3. Reste au dev : points 3 et 5.

**Correction du point 1 (25/09, compose du jour)** : `embedding-service` **écrit des fichiers** — la classe partagée `common_utils/embedding/Embedding.py:18-35` crée `/logs` et y tient `embeddings.log` et `temps_embedding.log` en rotation quotidienne, **30 jours conservés**. Sur la VM : dans le conteneur (aucun volume). Sur GKE : dans la couche inscriptible du pod, donc sur le disque éphémère du nœud, sans limite → **ajouter un `emptyDir` `/logs` avec `sizeLimit`** (même principe que `/app/tracking` des QC). Compose du jour : variables d'`embedding-service` = `DATABASE_SERVICE_URL`, `EMBEDDING_SERVICE_URL`, `GRPC_TIMEOUT`, `LLM_SERVICE_URL`, `PREFETCH_COUNT`, `PROCESS_TIMEOUT`, `RERANKING_SERVICE_URL`, `SERVICE_NAME` ; seul le client embedding est utilisé par le code (les trois autres adresses sont inertes).

**25/09 (fin)** : `emptyDir` `/logs` (1Gi) appliqué en shadow sur `embedding-service` (`kubectl diff` = volume + montage seuls ; `embeddings.log` et `temps_embedding.log` créés dans le volume ; pod 0 restart) — commit infra `baba9927`. Instantané du compose VM du 25/09 versionné (`infra-microservices/docker-compose.vm.yml`, commit `24be2cc1`, aucun secret en dur). 16 commits infra non poussés.
