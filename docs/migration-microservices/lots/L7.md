# Lot L7 — B1 cluster gateway + B3 always-on — bloc indivisible

> **Lot d'exécution 7/7** · risque **Fort** · date proposée **mer 30/09** *(à confirmer par la décision du 18/09)*.
> Procédure commune : [`../procedure-bascule-un-lot.md`](../procedure-bascule-un-lot.md) · plan : [`../plan-bascule-par-lots.md`](../plan-bascule-par-lots.md) · suivi : [`../suivi-bascule-par-lots.md`](../suivi-bascule-par-lots.md).
> ⚠️ **L1…L7 sont des lots d'exécution** ; ils ne couvrent que les priorités **P1, P2, P3** de l'inventaire. Ne pas confondre avec P1…P10.

## Composition

| Service (nom `docker-compose`) | Réplicas VM | Déploiement GKE (nom exact) | Secret à repointer | Cible réplicas GKE au départ |
|---|:--:|---|---|:--:|
| `api-gateway-go-service` | — | `api-gateway-go` | `(DSN gateway_db)` | — |
| `mcp-gateway-service` | — | `mcp-gateway-service` | `(DSN gateway_db + tokens)` | — |
| `mcp-zoho-service` | — | `mcp-zoho-service` | `(DSN gateway_db + tokens)` | — |
| `mcp-google-templates-runner` | — | `mcp-google-templates-runner` | `(tokens)` | — |
| `graph-rag-api-admin-service` | — | `graph-rag-api-admin-service` | `(NEO4J_URI)` | — |
| `graph-rag-dlq-manager-service` | — | `graph-rag-dlq-manager` | `graph-rag-dlq-manager-rabbitmq` | — |
| `dlq-manager-service` | — | `dlq-manager-service` | `(ELASTICSEARCH_URL + ES_USERNAME/PASSWORD)` | — |

**7 services.** Ce lot ne suit pas la mécanique consumer — voir « Nature de ce lot ».

## Ce qui bascule dans ce lot

- broker RabbitMQ dev→prod (`10.0.1.217` → `10.0.1.216`)
- Elasticsearch prod — règles d'archivage validées par le LEAD avant
- MySQL `gateway_db` prod via l'enabler `10.11.0.2:3307`
- Neo4j **écriture** → prod (`10.0.1.218`) — snapshot la veille, message contrôlé

## À savoir, service par service

| Service | Vigilance |
|---|---|
| `api-gateway-go-service` | B1. Service in-cluster 8500. DSN → `gateway_db` **prod** via l'enabler `10.11.0.2:3307`. Le jumeau VM **reste up** : il sert `api.hellopro.eu` jusqu'à la vague 2. |
| `mcp-gateway-service` | B1. ⚠️ **Pas de Service Kubernetes** à ce jour → à créer, sinon `templates-runner` ne le résout pas. Jumeau VM reste up (`mcp.hellopro.eu`). |
| `mcp-zoho-service` | B1. Même manque de Service. Dépend de MySQL au démarrage. |
| `mcp-google-templates-runner` | B1. Service in-cluster 8595. Conteneur VM **sans** préfixe `rag-hp-pub-`. |
| `graph-rag-api-admin-service` | B3. Écrit dans Neo4j → `10.0.1.218`. Un message contrôlé avant d'ouvrir. |
| `graph-rag-dlq-manager-service` | B3. ⚠️ Nom GKE **sans** `-service`. |
| `dlq-manager-service` | B3. **Boucle d'auto-archivage destructive** : le LEAD valide `dlq_auto_archive_rules` **avant** le câblage ES prod. ⚠️ Le jumeau VM tourne aussi (profil de l'override) → **décider de l'arrêter** pour ne pas avoir deux boucles sur le même ES. |

## Nature de ce lot

Ce n'est **pas** une bascule de consumers. Les quatre services B1 et `graph-rag-api-admin` sont des services HTTP : leurs jumeaux VM **continuent de servir** `api.hellopro.eu` et `mcp.hellopro.eu` jusqu'à la vague 2. Ce lot **câble les services GKE sur les données de production** (MySQL `gateway_db`, Neo4j, Elasticsearch, broker) sans arrêter la VM.

Deux décisions à prendre **avant** ce lot :

1. **`dlq-manager-service`** : le jumeau VM porte la même boucle d'auto-archivage. Deux boucles sur le même Elasticsearch prod = actions en double. **Arrêter le jumeau VM au moment du câblage**, ou ne pas câbler.
2. **Cohérence des jetons** : `ENCRYPTION_KEY`, `GOOGLE_TEMPLATES_RUNNER_ADMIN_TOKEN`, `ZOHO_GATEWAY_TOKEN` identiques entre gateway et satellites, **et** identiques à ceux du jumeau VM puisque les deux liront la même base.

Prérequis technique : créer les **Services Kubernetes** manquants pour `mcp-gateway-service` et `mcp-zoho-service`, et **jeter** la copie `gateway-mysql-shadow` (Deployment + PVC) une fois la base prod câblée.

## Prérequis spécifiques au lot (en plus de la procédure commune)

- Snapshot **Neo4j prod** la veille. Préparer **un message contrôlé** dont on connaît le résultat attendu dans le graphe.
- Revue des `dlq_auto_archive_rules` par le LEAD, **par écrit**, avant tout câblage ES.
- Dump `gateway_db` prod la veille. Services Kubernetes `mcp-gateway-service` / `mcp-zoho-service` créés.

## Ce qu'on attend du LEAD et des devs concernés

- Pré-contrôle de chaque service remis **avant la veille** (fiche du [`guide-pre-check-service.md`](../guide-pre-check-service.md)).
- Un référent joignable pendant la fenêtre (2 h) et le lendemain matin.
- Après bascule : un scénario métier de bout en bout sur la chaîne, et confirmation que les écritures attendues sont là — **et pas en double**.

## Feuille de lot

| Étape | Heure | Résultat | Par |
|---|---|---|---|
| Pré-flight (contexte, listes dérivées, comptes) | | | |
| État de référence broker | | | |
| Arrêt des jumeaux VM | | | |
| Repointage des secrets + rollout | | | |
| Broker : consumers > 0, backlog décroît | | | |
| Montée en réplicas (cible de départ) | | | |
| Validation fonctionnelle LEAD/dev | | | |
| GO observation 24 h | | | |
| **Décision J+1 matin : lot suivant / prolonger / rollback** | | | |

