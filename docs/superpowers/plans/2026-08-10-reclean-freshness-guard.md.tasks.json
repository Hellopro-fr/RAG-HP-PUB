{
  "planPath": "docs/superpowers/plans/2026-08-10-reclean-freshness-guard.md",
  "tasks": [
    {
      "id": 78,
      "subject": "Task 1: predicat partage archive_freshness_verdict",
      "status": "pending",
      "description": "Plan : `docs/superpowers/plans/2026-08-10-reclean-freshness-guard.md` (RAG-HP-PUB `features/poc`, cwd `apps-microservices/crawler-service`). Spec : `docs/superpowers/specs/2026-08-10-reclean-freshness-guard-design.md`. Suivre les 8 steps du plan dans l'ordre — TDD, et la référence de suite est prise au Step 1 AVANT toute modification.\n\n**Goal:** Extraire la comparaison de fraîcheur des conditions 4-5 de `classify` en fonction pure réutilisable, sans changer le comportement du repair.\n\n**Files:**\n- Modify: `apps-microservices/crawler-service/app/core/archived_status_repair.py` (nouvelle fonction avant `classify:33` ; conditions 4-5 remplacées à `:82-85`)\n- Test: `apps-microservices/crawler-service/tests/test_archived_status_repair.py`\n\n**Acceptance Criteria:**\n- [ ] `archive_freshness_verdict(log_mtime, snapshot_mtime)` existe au niveau module et rend les **constantes existantes** `NO_SNAPSHOT` / `RUN_AFTER_ARCHIVE` (`:22-23`), jamais de chaîne nouvelle.\n- [ ] `snapshot_mtime is None` → `NO_SNAPSHOT`, et ce cas **précède** le test sur `log_mtime` (l'ordre est le contrat du dry-run : un blob est compté dans son PREMIER motif d'échec).\n- [ ] `log_mtime is None` → `RUN_AFTER_ARCHIVE`.\n- [ ] `log_mtime >= snapshot_mtime` → `RUN_AFTER_ARCHIVE` (`>=`, pas `>`).\n- [ ] `log_mtime < snapshot_mtime` → `None`.\n- [ ] `classify` retourne ce verdict tel quel à la place de ses conditions 4 et 5, et l'ordre global des sept motifs est inchangé.\n- [ ] **Les 15 tests existants de `tests/test_archived_status_repair.py` passent sans qu'une seule ligne soit modifiée.** C'est le critère de neutralité.\n- [ ] Le module reste sans I/O et sans import de framework.\n\n**Verify:** `cd apps-microservices/crawler-service && python -m pytest tests/test_archived_status_repair.py -v` → 15 existants + 6 nouveaux passent, et `git diff --stat tests/test_archived_status_repair.py` ne montre que des insertions.\n\nGOTCHAs : `Optional` est déjà importé (`:10`) et le module est déjà importé sous l'alias `asr` dans le test — ne rien ajouter aux imports. Le commit se fait par `cat > commit_msg.txt` puis `git commit -F commit_msg.txt` ; `-F-` échoue sur cette machine et un `/tmp/…` absolu n'est pas compris par le git de Windows. **Ne PAS pousser.**\n\n```json:metadata\n{\"files\": [\"apps-microservices/crawler-service/app/core/archived_status_repair.py\", \"apps-microservices/crawler-service/tests/test_archived_status_repair.py\"], \"verifyCommand\": \"cd apps-microservices/crawler-service && python -m pytest tests/test_archived_status_repair.py -v\", \"acceptanceCriteria\": [\"archive_freshness_verdict rend les constantes existantes NO_SNAPSHOT/RUN_AFTER_ARCHIVE\", \"snapshot absent -> NO_SNAPSHOT, evalue AVANT le test sur log_mtime\", \"log absent -> RUN_AFTER_ARCHIVE\", \"log >= snapshot -> RUN_AFTER_ARCHIVE (>= et non >)\", \"log < snapshot -> None\", \"classify retourne le verdict tel quel, ordre des 7 motifs inchange\", \"les 15 tests existants passent SANS modification (critere de neutralite)\", \"le module reste pur : aucune I/O, aucun import de framework\"], \"modelTier\": \"mechanical\", \"plan\": \"docs/superpowers/plans/2026-08-10-reclean-freshness-guard.md\", \"spec\": \"docs/superpowers/specs/2026-08-10-reclean-freshness-guard-design.md\", \"repo\": \"RAG-HP-PUB\", \"branch\": \"features/poc\", \"tags\": [\"crawler-service\", \"archivage\"]}\n```"
    },
    {
      "id": 79,
      "subject": "Task 2: brancher le garde dans le sweep + synthese",
      "status": "pending",
      "blockedBy": [
        78
      ],
      "description": "Plan : `docs/superpowers/plans/2026-08-10-reclean-freshness-guard.md` (RAG-HP-PUB `features/poc`, cwd `apps-microservices/crawler-service`). Suivre les 7 steps du plan dans l'ordre. **Le Step 1 est un prérequis, pas un confort** : sans lui, tous les tests reclean existants échouent.\n\n**Goal:** `_reclean_archived_leftovers` appelle le prédicat après `active_prev_ids` et avant son contrôle d'âge, ne supprime rien sur un verdict non-`None`, et émet une synthèse par tick.\n\n**Files:**\n- Modify: `apps-microservices/crawler-service/app/core/crawler_manager.py` (garde inséré après `:4229` et avant le bloc d'âge `:4230-4235` ; compteurs près de `recleaned = 0` à `:4212` ; synthèse avant `return recleaned` à `:4245`)\n- Test: `apps-microservices/crawler-service/tests/test_crawler_manager_reclean.py`\n\n**Acceptance Criteria:**\n- [ ] Le garde s'exécute **après** le test `active_prev_ids` et **avant** le test d'âge — l'ordre est un choix d'observabilité : si l'âge rejetait d'abord, les cas dangereux qu'il sauve ne seraient jamais comptés comme dangereux.\n- [ ] Verdict non-`None` ⇒ `_cleanup_local_data` **n'est pas appelé** pour ce crawl, et le compteur correspondant est incrémenté.\n- [ ] Un `OSError` autre que `FileNotFoundError` au `stat` d'un sidecar ⇒ avertissement puis `continue`, sans suppression — même traitement explicite que le repair (`:4128-4132`, « skipping, not rejecting »).\n- [ ] Une ligne `ARCHIVED_LEFTOVER_RECLEAN_SUMMARY actioned=N skipped_tree_newer=X skipped_no_snapshot=Y` est émise quand au moins un crawl a été traité ou refusé.\n- [ ] `_make_archived_dir` donne à `crawler.log` un mtime **strictement antérieur** à celui de `_status_snapshot.json`, de façon déterministe.\n- [ ] Les tests reclean préexistants passent, y compris ceux qui attendent une suppression.\n- [ ] Suite complète : `failed` inchangé par rapport à la référence prise au Step 1 de la tâche 1.\n\n**Verify:** `cd apps-microservices/crawler-service && python -m pytest tests/test_crawler_manager_reclean.py -v` → tous passent. Puis `python -m pytest tests/ -q` → `failed` identique à la référence (1, `test_archive_mock_e2e.py::TestArchiveMockE2E::test_daemon_logic`, préexistant et vérifié identique sur la base — ne pas le chasser).\n\nGOTCHAs : `archived_status_repair` et `_mtime_or_none` sont déjà disponibles dans `crawler_manager.py` (`classify` appelé à `:4136`, `_mtime_or_none` défini à `:83`) — ne rien ajouter aux imports. Commit par `cat > commit_msg.txt` puis `git commit -F commit_msg.txt`. **Ne PAS pousser.**\n\n```json:metadata\n{\"files\": [\"apps-microservices/crawler-service/app/core/crawler_manager.py\", \"apps-microservices/crawler-service/tests/test_crawler_manager_reclean.py\"], \"verifyCommand\": \"cd apps-microservices/crawler-service && python -m pytest tests/test_crawler_manager_reclean.py -v\", \"acceptanceCriteria\": [\"le garde s'execute apres active_prev_ids et AVANT le controle d'age\", \"verdict non-None => _cleanup_local_data non appele + compteur incremente\", \"OSError non-FileNotFoundError => warning puis continue, sans suppression\", \"ligne ARCHIVED_LEFTOVER_RECLEAN_SUMMARY emise avec les deux compteurs\", \"_make_archived_dir horodate crawler.log strictement avant le snapshot\", \"les tests reclean preexistants passent, y compris ceux attendant une suppression\", \"suite complete : failed inchange par rapport a la reference\"], \"modelTier\": \"standard\", \"plan\": \"docs/superpowers/plans/2026-08-10-reclean-freshness-guard.md\", \"spec\": \"docs/superpowers/specs/2026-08-10-reclean-freshness-guard-design.md\", \"repo\": \"RAG-HP-PUB\", \"branch\": \"features/poc\", \"tags\": [\"crawler-service\", \"archivage\"]}\n```"
    }
  ],
  "lastUpdated": "2026-08-10T17:05:00Z"
}
