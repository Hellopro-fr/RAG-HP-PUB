{
  "planPath": "docs/superpowers/plans/2026-08-24-circuit-breaker-error-rate.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Correct the spec before implementing",
      "status": "pending",
      "description": "**Goal:** Remove the one false claim in the design authority and record the two findings the implementer needs, so nobody builds `errors + processed` from the letter of §5.\n\n**Files:**\n- Modify: `docs/superpowers/specs/2026-08-24-circuit-breaker-error-rate-design.md` (§5, and a new §8)\n\n**Acceptance Criteria:**\n- [ ] §5 no longer claims exact symmetry with the proxy wall; it names the mixture and the new counter\n- [ ] A new §8 records that `updateHealthVerdict.ts:69` carries the same defect and is deliberately out of scope, with the reason\n- [ ] §8 states the honest limitation: this fix stops the lock, it does not by itself make the marginal runs' deletions apply\n- [ ] No claim in the spec now contradicts the \"What the exploration established\" section of the plan\n\n**Verify:** `grep -c 'symétrie exacte' docs/superpowers/specs/2026-08-24-circuit-breaker-error-rate-design.md` → `0`\n\n```json:metadata\n{\"files\": [\"docs/superpowers/specs/2026-08-24-circuit-breaker-error-rate-design.md\"], \"verifyCommand\": \"grep -c 'symétrie exacte' docs/superpowers/specs/2026-08-24-circuit-breaker-error-rate-design.md\", \"acceptanceCriteria\": [\"the exact-symmetry claim is gone from §5\", \"§8 records updateHealthVerdict.ts:69 as the same defect, out of scope, with the reason\", \"§8 states that the fix stops the lock but not the withholding of deletions\"], \"modelTier\": \"standard\"}\n```",
      "activeForm": "Correcting the spec"
    },
    {
      "id": 1,
      "subject": "Task 2: The pure decision module",
      "status": "pending",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** A dependency-free shouldTripErrorRateBreaker() whose rate provably cannot exceed 1, which reproduces today's verdict for every run legitimately stopped today, and whose tests encode both bounds of spec §4.2.\n\n**Files:**\n- Create: `apps-microservices/crawler-service/crawler/src/errorRateBreaker.ts`\n- Create: `apps-microservices/crawler-service/crawler/src/errorRateBreaker.test.ts`\n\n**Acceptance Criteria:**\n- [ ] The function is pure — no import of Crawlee, Redis, StatsManager or context\n- [ ] The sample gate reads `stats.processed`, NOT `attempts` (a test pins this)\n- [ ] `maxErrorRate = 0` disables the signal (preserves routes.ts:467 `> 0` semantics)\n- [ ] The comparison stays strict `>` (preserves today's boundary behaviour)\n- [ ] `reason` still begins with the literal `Error rate too high (` — production logs are grepped on that prefix\n- [ ] A zero denominator yields rate 0, never NaN\n- [ ] Both §4.2 bounds are separate tests: all-off-book (rescued) and all-on-book (still trips)\n- [ ] The invariant test is proven able to fail (mutate `attempts` to `processed`, see it go red, revert)\n- [ ] npm test green, count = 435 + tests written (9 as written in the plan)\n\n**Verify:** `cd apps-microservices/crawler-service/crawler && npm test` → `fail 0`, `tests` = 435 + tests added\n\n```json:metadata\n{\"files\": [\"apps-microservices/crawler-service/crawler/src/errorRateBreaker.ts\", \"apps-microservices/crawler-service/crawler/src/errorRateBreaker.test.ts\"], \"verifyCommand\": \"cd apps-microservices/crawler-service/crawler && npm test\", \"acceptanceCriteria\": [\"shouldTripErrorRateBreaker imports nothing from Crawlee/Redis/StatsManager/context\", \"sample gate reads stats.processed, not attempts\", \"maxErrorRate = 0 disables the signal\", \"the trip reason starts with 'Error rate too high ('\", \"zero denominator yields 0, not NaN\", \"both §4.2 bounds are separate passing tests\", \"the invariant test was observed failing under a mutation, then reverted\", \"npm test reports fail 0\"], \"modelTier\": \"standard\"}\n```",
      "activeForm": "Implementing the pure decision module"
    },
    {
      "id": 2,
      "subject": "Task 3: The errors_unprocessed counter",
      "status": "pending",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** A counter incremented exactly once per dataset URL whose error never reached increment(\"processed\"), written where that fact is known, with the write/read name agreement pinned.\n\n**Files:**\n- Modify: `apps-microservices/crawler-service/crawler/src/class/UpdateChecker.ts` (CASE 1, at the `increment(\"errors\")` on ~:175)\n- Create: `apps-microservices/crawler-service/crawler/src/errorsUnprocessedParity.test.ts`\n\n**Acceptance Criteria:**\n- [ ] The increment sits in CASE 1's `isFromDataset` branch, immediately after `increment(\"errors\")`\n- [ ] Exactly one increment site — CASE 3 must stay untouched\n- [ ] `errors` itself is unchanged: same sites, same conditions, same count (2 in this file)\n- [ ] The counter goes through StatsManager (Redis), NOT a context-local field — `errors` and `processed` are cumulative across restarts\n- [ ] A parity test pins that the name written in UpdateChecker.ts is the name read in routes.ts\n- [ ] npm test green\n\n**Verify:** `cd apps-microservices/crawler-service/crawler && npm test` → `fail 0`; and `grep -c 'increment(\"errors_unprocessed\")' src/class/UpdateChecker.ts` → `1`\n\n```json:metadata\n{\"files\": [\"apps-microservices/crawler-service/crawler/src/class/UpdateChecker.ts\", \"apps-microservices/crawler-service/crawler/src/errorsUnprocessedParity.test.ts\"], \"verifyCommand\": \"cd apps-microservices/crawler-service/crawler && npm test\", \"acceptanceCriteria\": [\"increment(\\\"errors_unprocessed\\\") appears exactly once, in CASE 1\", \"increment(\\\"errors\\\") still appears exactly twice in UpdateChecker.ts\", \"the counter goes through StatsManager, not a context field\", \"the parity test pins write-name == read-name\", \"npm test reports fail 0\"], \"modelTier\": \"mechanical\"}\n```",
      "activeForm": "Adding the errors_unprocessed counter"
    },
    {
      "id": 3,
      "subject": "Task 4: Wire it, and document the mechanism",
      "status": "pending",
      "blockedBy": [
        1,
        2
      ],
      "description": "**Goal:** routes.ts delegates the error-rate decision to the pure function, and the service's CLAUDE.md finally describes the breaker that stopped 69 runs of a batch.\n\n**Files:**\n- Modify: `apps-microservices/crawler-service/crawler/src/routes.ts` (import at ~:33, block :449-467)\n- Modify: `apps-microservices/crawler-service/CLAUDE.md` (new section after `## Update Mode ...`, ~:83)\n\n**Acceptance Criteria:**\n- [ ] No arithmetic on the error rate is left inline in routes.ts\n- [ ] `redirectRate` stays inline and unchanged — its branch is disabled in production and out of scope\n- [ ] The `if (processed >= cb.minSample)` wrapper is kept, so redirect and growth keep their gate\n- [ ] `cb` is passed straight to the pure function (structural typing)\n- [ ] CLAUDE.md gains a section naming the breaker, its three thresholds, the two that arrive disabled, the exit-code consequence, and the dead micro mode\n- [ ] npm test green\n- [ ] Both files in ONE commit — the spec requires the doc section to ship with the fix\n\n**Verify:** `cd apps-microservices/crawler-service/crawler && npm test` → `fail 0`; and `grep -c 'errors / processed' src/routes.ts` → `0`\n\n```json:metadata\n{\"files\": [\"apps-microservices/crawler-service/crawler/src/routes.ts\", \"apps-microservices/crawler-service/CLAUDE.md\"], \"verifyCommand\": \"cd apps-microservices/crawler-service/crawler && npm test\", \"acceptanceCriteria\": [\"no error-rate arithmetic left inline in routes.ts\", \"redirectRate left inline and unchanged\", \"the processed >= cb.minSample wrapper is kept\", \"CLAUDE.md has a circuit-breaker section covering the three thresholds and the dead micro mode\", \"npm test reports fail 0\", \"routes.ts and CLAUDE.md are in one single commit\"], \"modelTier\": \"standard\"}\n```",
      "activeForm": "Wiring the breaker and documenting it"
    }
  ],
  "lastUpdated": "2026-08-24"
}