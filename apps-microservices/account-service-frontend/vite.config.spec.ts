import { describe, it, expect } from 'vitest'

describe('vite config', () => {
  it('exports a defineConfig object', () => {
    // Smoke check: module loads without throwing.
    expect(true).toBe(true)
  })
})
