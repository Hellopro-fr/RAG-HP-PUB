package oauth2

import "testing"

func TestHandlersMovedToAuthserver(t *testing.T) {
	// OAuth2 Authorization Server logic has moved to internal/authserver/.
	// This test file is a placeholder — the real tests are in authserver/*_test.go.
	t.Log("handlers moved to internal/authserver/")
}
