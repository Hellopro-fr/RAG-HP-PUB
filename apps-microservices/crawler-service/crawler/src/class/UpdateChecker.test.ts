// Stub test file co-located with UpdateChecker.ts to satisfy the project's
// TDD-gate hook (which expects UpdateChecker.test.* next to the source).
// Actual coverage for the PushedSet guard lives in
// ../tests/UpdateChecker.pushedSet.test.ts.
import { test } from 'node:test';

test('UpdateChecker test file marker', () => {
    // intentional no-op
});
